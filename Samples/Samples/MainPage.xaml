<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Samples"
             xmlns:rs="clr-namespace:Xamarin.RSControls.Controls;assembly=Xamarin.RSControls"
             xmlns:validators="clr-namespace:Xamarin.RSControls.Validators;assembly=Xamarin.RSControls"
             x:Class="Samples.MainPage">

    <StackLayout>
        
        <rs:RSSvgImage Source="Samples/Data/SVG/calendar.svg" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand"/>

        <Label Text="{Binding NumericEntryValue}"></Label>


        <rs:RSNumericEntry Placeholder="RSNumericEntry" Value="{Binding NumericEntryValue}" NumberDecimalDigits="2" CustomUnit="$" HideTrailingZeros="True">
            <rs:RSNumericEntry.Behaviors>

                <validators:ValidationBehaviour PropertyName="Value">
                    <validators:ValidationBehaviour.Validators>

                        <validators:RequiredValidation/>
                        <validators:FormatValidation Format="^[0-9]{6}$"/>

                    </validators:ValidationBehaviour.Validators>
                </validators:ValidationBehaviour>

            </rs:RSNumericEntry.Behaviors>
        </rs:RSNumericEntry>

    </StackLayout>

</ContentPage>
