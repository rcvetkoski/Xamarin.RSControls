<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Samples"
             xmlns:rs="clr-namespace:Xamarin.RSControls.Controls;assembly=Xamarin.RSControls"
             xmlns:validators="clr-namespace:Xamarin.RSControls.Validators;assembly=Xamarin.RSControls"
             xmlns:enums="clr-namespace:Xamarin.RSControls.Enums;assembly=Xamarin.RSControls"
             x:Class="Samples.MainPage">

    <ScrollView Orientation="Vertical">
        <StackLayout Margin="20" x:Name="stack">

            <!--<rs:RSEntry Placeholder="Entry">
                <rs:RSEntry.Behaviors>
                    <validators:ValidationBehaviour PropertyName="Text">
                        <validators:ValidationBehaviour.Validators>
                            <validators:RequiredValidation/>
                        </validators:ValidationBehaviour.Validators>
                    </validators:ValidationBehaviour>
                </rs:RSEntry.Behaviors>
            </rs:RSEntry>

            <rs:RSNumericEntry Placeholder="Numeric Entry" CustomUnit="KG" NumberDecimalDigits="3" HasBorder="True">
                <rs:RSNumericEntry.Behaviors>
                    <validators:ValidationBehaviour PropertyName="Text">
                        <validators:ValidationBehaviour.Validators>
                            <validators:RequiredValidation/>
                        </validators:ValidationBehaviour.Validators>
                    </validators:ValidationBehaviour>
                </rs:RSNumericEntry.Behaviors>
            </rs:RSNumericEntry>

            
            <rs:RSNumericEntryInputLayout NumberDecimalDigits="2" CustomUnit="€" Placeholder="Input text layout Numeric Entry">

            </rs:RSNumericEntryInputLayout>

            
            <rs:RSEnumPicker x:TypeArguments="enums:DateSelectionModeEnum" Placeholder="Date selection mode" HasBorder="True">
                
            </rs:RSEnumPicker>

            <rs:RSDatePicker HasBorder="True" DateSelectionMode="MonthYear">
                
            </rs:RSDatePicker>

            <rs:RSDatePickerInputLayout>
                
            </rs:RSDatePickerInputLayout>

            <rs:RSPickerInputLayout ItemsSource="{Binding ObsCollectionPicker}" Placeholder="Obs collection picker" >
                <rs:RSPickerInputLayout.Behaviors>
                    <validators:ValidationBehaviour PropertyName="SelectedItem">
                        <validators:ValidationBehaviour.Validators>
                            <validators:RequiredValidation/>
                        </validators:ValidationBehaviour.Validators>
                    </validators:ValidationBehaviour>
                </rs:RSPickerInputLayout.Behaviors>
            </rs:RSPickerInputLayout>


            <rs:RSEnumPickerInputLayout x:TypeArguments="enums:DateSelectionModeEnum" Placeholder="Test enum input layout">
                
            </rs:RSEnumPickerInputLayout>

            <rs:RSSearchView Placeholder="Search" CancelButtonColor="Red" ItemsSource="{Binding ListPickerEnum}" SelectedItem="{Binding SelectedDateMode}">
                
            </rs:RSSearchView>

            <Label Text="{Binding SelectedDateMode}"/>

            <SearchBar Placeholder="Forms"></SearchBar>

            <Button Text="Switch to Page1" Clicked="Button_Clicked"></Button>

            <Button Text="Remove item" Clicked="Button_Clicked2"></Button>

            <Entry></Entry>-->

            <Button Text="Switch to Page1" Clicked="Button_Clicked"></Button>


                        <rs:RSNumericEntry Placeholder="Numeric Entry" CustomUnit="KG" NumberDecimalDigits="3" HasBorder="False">
                <rs:RSNumericEntry.Behaviors>
                    <validators:ValidationBehaviour PropertyName="Text">
                        <validators:ValidationBehaviour.Validators>
                            <validators:RequiredValidation/>
                        </validators:ValidationBehaviour.Validators>
                    </validators:ValidationBehaviour>
                </rs:RSNumericEntry.Behaviors>
            </rs:RSNumericEntry>


            <rs:RSPicker x:Name="rsPicker" ItemsSource="{Binding ListPicker}" DisplayMemberPath="Name" OrderBy="Ascending" SelectedItem="{Binding SelectedPerson}" Placeholder="Single pick Custom template with validators" HasBorder="True">

            </rs:RSPicker>

            <Picker ItemsSource="{Binding ListPicker}"></Picker>

            <DatePicker></DatePicker>

        </StackLayout>
    </ScrollView>

</ContentPage>
